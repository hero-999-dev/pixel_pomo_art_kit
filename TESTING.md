# Testing — Pixel Pomo Art Kit

## Running the suite

```
python -m unittest discover -s tests -v
```

**412 tests, all passing.** Verified 2026-09-25 on `main` (v2.9.0),
Windows 11, Python 3.14.4, Tk 8.6, Pillow 12.2.0, `PIXEL_POMO_TOOLS` unset (i.e.
against the sibling game checkout the kit resolves relatively,
`..\App\flutter\tools\gen_objects.py`). About 260 s wall clock, almost all of
it the 198 smoke tests each building the whole window. Nothing in the suite
touches the network: the updater is tested against a fake opener.

Everything is plain `unittest` — no third-party test framework, no fixtures
beyond `tempfile.TemporaryDirectory`, no mocked filesystem. The release
workflow runs the suite on Windows, Apple Silicon and Intel macOS before it
builds anything. **Test Art Kit** (`.github/workflows/test.yml`) runs the same
suite on the same three runners by hand, building and publishing nothing:
optional space-separated `-k` patterns, `-X faulthandler`, and on a Mac the
crash report's native frames if Tk takes the process down. On a Mac the smoke
tests keep their root on screen rather than withdrawn (`_tk_or_skip`): Tk 8.6
there does not survive a suite of withdrawn roots (v2.8.0).

## What each file covers

| File | Covers | Tests |
|---|---|---|
| `tests/test_model.py` | SWAP (#v2.9.0): a letter and a raw colour that look alike both match, a swap changes and counts them all and never touches an empty cell, a region keeps it inside and is clipped, `colour_of`. `Palette` (the nine letters resolving to RGBA). `Drawing` (blank/paint/erase/bounds, `copy()` not sharing rows, and that painting a raw colour flips a drawing from `"letters"` to `"pixels"`). `History` (a whole drag is one undo step, undo/redo restore the exact prior grid, a no-op stroke isn't recorded, redo's branch is dropped by a new stroke after an undo, undo/redo are no-ops at either end, the 200-entry cap). Also that `Drawing.kind` is derived from the cells; that `flood()` fills exactly the connected region and no-ops on its own value; that `resize()` pads right/below and crops right/below without touching the art; and that `History.repaint()` puts a palette change on every snapshot. **#v2.6.0:** `label` defaults empty and survives `copy()`; `count` / `row_count` / `col_count` (out-of-range rows give 0); `region()` copies a rectangle given either corner order and is a copy, not a view; `stamp()` skips empty cells, clips at the edge, and can be told not to skip; `clear_region()`. | 30 |
| `tests/test_engine_io.py` | The forest props (#v34.8): every tree/bush/rock the engine loads is in the library, a tree opens at the canvas size its tile count implies, props export under their own filename with no `flower_` prefix, an untouched prop is byte-identical to the shipped PNG, an off-size tree is refused, and rendering one returns it unchanged. Importing the 24 shipped flowers live from `gen_objects.py`. Rendering equals the engine's own `flower_variant`/`rose_variant` output. Every export function and every `ExportRefused` guard; `engine_sprite_names` names exactly the files `export_engine_sprite` writes; `export_palette_literal` reproduces the engine's `_FLOWER_PALS` line. Every species and forest kind has an English display name (#v2.2.0). PNG import: profile → sRGB, alpha snapping, nearest-neighbour downscale, the x16-only scale guess. **#v2.6.0:** `export_sprite` writes any size at x16 while the strict `export_engine_sprite` still refuses it; `suggested_sprite_name` gives the engine name for shipped art and a slug (or `sprite.png`) otherwise; `export_png(grid=…)` draws lines on every boundary including the last pixel row/column, leaves cells and transparency alone, and draws nothing when not asked; `default_label` for flowers/props/nothing, and everything seeded is labelled. | 54 |
| `tests/test_store.py` | The designs that ship with the kit (#v2.9.0): the bundle is HeroDev999's and LadyOfDynamite's four, never the Copy Siberian study; seeded once and written to disk, a deleted one not put back, no twin of one already there. The JSON codec round-tripping a letters, a raw-pixel and a mixed drawing. Every malformed-file `CorruptDrawing` path. Atomic save. `Library`: seeding, reopening, duplicate/remove/rename, a corrupt file is skipped rather than failing the load, value-equal drawings are not confused on removal, the session's first save leaves a `.json.bak` (of the file as it was on DISK). **#v2.6.0:** the label round-trips; a file without one, or with a non-string one, reads as unlabelled and whitespace is stripped; loading a pre-label library gives seeded kinds their label, leaves the artist's own unlabelled, and `labels()` lists the distinct ones. | 33 |
| `tests/test_symmetry.py` | The symmetry bar (#v2.4.0), pure geometry: a vertical bar covers `length` rows centred on the click (odd and even lengths), a horizontal one covers columns; each reports its angle (90/180); a vertical bar mirrors left↔right only within its reach and never for a cell on the bar, a horizontal one top↔bottom; length is clamped and a bad orientation refused; `moved_to` keeps the shape; `expand()` leaves cells alone with no bar, appends each twin after its original without duplicates, and paints cells beyond the bar alone. REVERSE (#v2.9.0, in STICK's place): `turned()` swaps the rows across X, the columns across Y, both across X+Y, and leaves its input alone; `reverse_corner()` puts the copy right beside the selection at distance 0, leaves that many empty cells otherwise, moves a diagonal both ways (“15 toward the lower left”), takes X and Y apart, ignores the unused distance of a straight arrow, clamps distances and refuses a bad direction; `reverse_copy()` stamps the turned block without wiping what its empty cells cover, and counts what falls off the drawing. | 22 |
| `tests/test_settings.py` | `offered_designs` accumulates and persists (#v2.9.0). The preferences file (#v2.4.0): a missing file gives the six classic favourites, `draw`, and a vertical 5-long bar in mode `off`, and writes nothing until something changes; add/remove favourites round-trip through the file, case- and `#`-insensitively, without duplicates; a bad hex is refused; tool and symmetry persist (mode kept when not given, stored when given); an old file without a mode still reads; a corrupt file falls back to defaults instead of raising; a hand-edited bad favourite drops that entry only, and a wrong-typed field keeps its default. **#v2.6.0:** grid colours default to the matcha checker tones, validate, persist and reset; `show_grid` and the eraser size persist, the eraser clamped to 64. | 14 |
| `tests/test_paths.py` | Where the data lives (#v2.5.0): from source, the repo root; a frozen Windows build uses `%LOCALAPPDATA%\PixelPomoArtKit`, never the .exe's folder, and names that folder as the legacy place to migrate from; a frozen Mac uses Documents and has no legacy folder. `migrate_legacy()` copies drawings and `settings.json` from an old beside-the-exe folder and leaves it untouched, skips non-drawings, never overwrites a file already in the data folder, copies nothing on a second run, and copes with a missing legacy folder or with being handed the data folder itself. | 6 |
| `tests/test_updater.py` | Every request carries certifi's certificate store (#v2.9.0: CERTIFICATE_VERIFY_FAILED on macOS). Updates (#v2.5.0), all against a fake HTTP opener: `version.parse` handles `v` prefixes, two-digit components and `-rc` suffixes (leading digits only), and `VERSION` is a real triplet; `Release.is_newer` for newer/same/older tags; `check()` hits the releases API with a `PixelPomoArtKit` User-Agent and parses tag, page, notes and asset URLs; a non-JSON body raises rather than guesses; `download()` writes the file, reports progress up to the total, and leaves no `.part` behind; `backup_library()` zips every drawing and returns None for an empty library; `stage_windows()` unpacks the .exe from the zip beside the running one without touching it and writes a hand-off script naming this pid, the `.old.exe` it keeps, the two moves and the relaunch; a zip with no .exe is refused. | 10 |
| `tests/test_app_smoke.py` | The tkinter window wired up end-to-end against a real (hidden) `Tk()` root, with the library AND the settings file in a temp dir. From before: opens with every seeded drawing listed; a drag paints as one undoable stroke; the eraser; switching drawings keeps edits; zoom clamps; a picked colour turns a letters drawing into pixels; `set_ink` rejects what `render()` would drop; the panes are wired; the shade square sets a real ink; a frozen Mac build keeps drawings out of the bundle and a frozen Windows build writes beside the exe; the export dialog does not default into the game's assets; a fast drag fills the Bresenham line; right-click eyedrops and ignores empties; unknown tools are refused; the fill floods as one undo; a new drawing is 32x32 by default; an undo is written to disk. **New in #v2.4.0:** `save()` writes and reports success; the ink entry is an `Entry` showing the bare `#rrggbb` (no `ink:` prefix) and `set_ink_hex` accepts `RRGGBB`/`#rrggbb` and refuses anything else without changing the ink; a letter ink shows its palette colour; `+` adds the ink to favourites and `remove_favourite` takes it out of both settings and the grid; the ready and favourite grids and the hue strip are all `PICKER_W` wide; `set_tool` persists and a second app on the same settings opens with that tool; `new_drawing(cols, rows)` honours its size and `size_presets()` gives Flower 16 x the engine's bloom height, Bush 16x16, and trees at 32/48/64; `resize()` is one undo and the size label follows; with SYMMETRY on the first click places the bar and paints nothing, later strokes mirror within the bar's reach only, the mirror undoes with its stroke, and off means no mirror; orientation/length rebuild the bar and persist; a stroke updates one library row (the row widgets are the same objects) and the art is a single canvas image item; the help overlay toggles; `close()` writes a stroke that was still in progress and destroys the window once; the icon grid is a complete 16x16 sprite whose letters all have colours and that renders to a PhotoImage. **New in #v2.5.0:** a frozen Windows build writes to the user profile, not beside the .exe (the v2.4.0 test asserted the opposite); pressing on the bar drags it (with the grab offset kept) without painting or recording a stroke, and PLACE BAR re-arms click-to-place; `m` cycles OFF → MIRROR → STICK (REVERSE since #v2.9.0) and a bad mode is refused; `import_png` puts a PNG into the library saved, with a row, name from the file and species `""`; a newer `Release` turns the UPDATE button into `UPDATE ● vX`, a current one leaves it alone, and a failed silent check changes nothing. **#v2.7.0 to #v2.8.0** (see log.md for each pass): the camera strip, FREE / LOCK / the anchors and the zoom about the pointer; the viewport renderer and zooming below 1x to the corner's 1/n x; the kit's own menus and message boxes; the one checklist of labels and artists (what is ticked is what is listed, rows kept in order, its arrow turning while it is open); edge and corner resizing; the eraser's outline; Drawing Patch 1; the export dialog's signature and the export record; F12 snapshots in the TEST build only; a FIT that follows the pane; strokes written into the view's picture rather than the canvas; every heading and row menu re-read on a language change; the status corner as wide as its message; the colour pager at the far right; every dialog and menu shown only once placed; each drawing keeping its own symmetry, a line left outside taken away; LOOK showing the art plain and drawing nothing; GUIDE ending with how the work is protected, in the language of the moment; its rows ruled off and flush left, a drawn close button; the grid's WHITE and BLACK grounds and the captions beside their …; a label or an artist renamed or taken off on every drawing (the artist's colour following), from a ⋮ beside each name in their dialogs; the total pixel count; every tools-pane row as wide as the colour grids. **#v2.9.0:** REVERSE copies the selection turned over beside it as one undo, the original staying selected; across X and diagonally 15 away, and X · Y apart, the panel remembered; the button is ordinary with nothing selected, glows with a selection and is pressed as the mode, and with nothing selected it gets SELECT ready and refuses to copy; Enter copies under REVERSE; each mode shows its own panel and OFF none; eight arrows, EQUAL / X · Y swapping the distance fields, a straight arrow greying the unused one; the dashed outline where the copy will land, not outside REVERSE or under LOOK; LOOK as the third of the grid row - three equal boxes, one point size in every language, one pressed, a GRID button leading out of LOOK. Second pass: the middle square (or the pressed arrow) takes the arrow away and nothing is copied; REVERSE lets go when its selection is lifted and can be chosen again, and lets go when pressed again; R copies under REVERSE only; the preview lies under the cell lines. Third pass: the rulers head every column and row x1… / y1… over their own cells and stop at the drawing's edge, keep every cell's own box and label zoomed out, the type shrinking, mark the selection and the cell under the pointer; COORDS sits right of the readout and hides and remembers them; the readout counts from 1; LOOK is VIEW ONLY. Fifth pass: a heading selects its whole column or row, a drag along the ruler several (stopping at the last), past the end nothing, the corner everything, and not under VIEW ONLY. Sixth pass: SWAP changes every pixel of a colour as one undo, keeps to a selection made first, matches a letter and a raw colour that look alike, refuses an empty pixel, the ink itself and VIEW ONLY, outlines what it would change, and is the fifth tool. Seventh pass: the snapshot message is a link to its file until another message replaces it. Eighth pass: the colour panel shows the ink as H S B / R G B (Photoshop's numbers) and the colour it replaced, a drag being one change and WAS going back; typed numbers set the ink, a bad one is put back and a big one clamped; the square rings the ink and the strip follows its hue but not a grey's; the Mac icon has whole cells at every size. **New in #v2.6.0:** labels show on the row chip, `set_label` saves and trims, a label filter hides the other rows and names itself on the button, clearing it shows everything, and `_set_species` is gone; a drawing made under a filter takes that label and stays visible, an import whose label the filter would hide drops the filter; the eraser clears a centred W×H footprint; SELECT → Ctrl+C → paste at a cell → drag inside → nudge → Enter lands as one undo with the original intact and the placed block selected; pressing inside a selection lifts it (one undo) and a click outside drops it without painting (one undo each); cut and delete clear as strokes and copy with nothing selected is refused; dragging a bar's end resizes it and persists, dragging past the other end flips it, and nothing is painted; WITH/WITHOUT GRID adds/removes the `grid` items, grid colours round-trip into the entries, the checkerboard follows them (since #v2.8.0 as 2 × 2-cell squares in the art tile itself, where it had been 100 canvas items), DEFAULT restores; the strip counts total / row / column / selection pixels, reads the colour under the cursor, and lists the drawing's colours most-used first with counts. | 198 |
| `tests/test_i18n.py` | The four languages: a missing key falls back to English, an unknown language is English, a translation may only change a button's point size (never the family or the bold), and every key added in #v2.8.0 - the export background, the camera, the artist's mark, F12 - is in all four tables; every language has every key English has, with the same {placeholders}. | 7 |
| `tests/test_raster.py` | Grids to pictures (#v2.8.0): a grid that fits comes back untouched and a bigger one is averaged down to fit; a one-cell line survives a tenth of the size; a half-covered block is half as opaque, not darker; a block cut short by the edge averages what it has; the picture is the same with or without Pillow; laying a partly covered pixel on a colour mixes it; the PNG bytes are the same either way. | 8 |
| `tests/test_provenance.py` | The artist's mark on exports (#v2.8.0, sixth test pass): every letter and digit has a pixel glyph and the © fills the line; names fold to plain letters (Ş, ı, ł, ß); no signature without an artist or a choice; the corner signature sits small in the bottom right in its two colours; the watermark crosses the whole picture and is cut to it; both at once lay the watermark first and the corner over it; too small a picture is left alone and a long name steps down to a shorter form; stamping copies a shared row before touching it; alpha compositing; a PNG carries Title/Author/Copyright/XMP (UTF-8 when Latin-1 cannot hold the name) with its pixels unchanged, and nothing without a mark; with the name off, only what made it; every format says what made it (Software, the comment, the SVG's opening comment); a JPEG carries EXIF folded to ASCII plus XPAuthor as written, XMP and a comment; an SVG opens with its title and Dublin Core and ends with its signature group, still well-formed; the record chains every export to the one before, and an edit breaks the chain where it was made. | 20 |
| `tests/test_test_build.py` | `run_art_kit_test.py`, the TEST build's entry point: it writes to its own folder, not the artist's, starting from a copy of the real drawings; the real library is untouched by starting it and nothing written in the test folder reaches it; restarting does not clobber work done there, and a new real drawing shows up on the next run; the startup check never dials out (with a control proving the trap would fire); it cannot swap itself for a release binary; the window says it is a test build. | 10 |
| **Total** | | **412** |

## Not covered

- **How the window actually looks.** The smoke tests drive `ArtKitApp`
  through its grid-coordinate methods and assert on model state and widget
  wiring — never on pixel output or layout. The screenshots taken while
  building #v2.4.0 (main window, help overlay, symmetry bar, a 48-cell tree,
  the size dialog) were checked by eye on Windows and are not part of the
  suite. A misaligned pane or an unreadable label would not fail a test.
- **macOS, live.** This kit is developed on Windows. The suite runs on the
  macOS CI runners before every release, and the #v2.4.0 changes were written
  against known Aqua behaviour (native buttons ignore `bg`, right-click is
  `Button-2`, Cmd+Q bypasses `WM_DELETE_WINDOW`, Documents needs permission),
  but nobody has clicked through the Mac build as part of this release. The
  Documents-refused fallback in `__main__.data_dir()` in particular cannot be
  provoked from Windows and is untested beyond its own logic. v2.8.0's Mac
  fixes were found and checked on the CI runners - including one window
  through 150 rounds of message box, popup menu and export dialog, clean three
  times on each Mac - not by hand.
- **Interactive widget behaviour.** Double-click-to-select in the ink entry,
  right-click on a favourite opening a menu, the Spinbox arrows, hover colours
  on buttons, the ghost bar following the cursor — all exercised by hand, none
  by the suite, which calls the methods those events lead to instead.
- **`test_app_smoke.py` skips itself when there is no display.**
  `_tk_or_skip()` turns the `TclError` from `tkinter.Tk()` into
  `unittest.SkipTest`, so a headless machine reports its smoke tests as skipped,
  not failed. Here a real display was available, so they all ran.
- **Byte-exact export is sampled, not exhaustive.** The shipped-PNG comparison
  runs against three flowers and one forest prop; every drawing goes through
  the same `render()`/`export_engine_sprite()` code, so this is strong
  evidence for all of them, not individual proof of each one.
- **The Windows self-update, end to end.** The suite proves the pieces
  (download, backup, staging, the script's text). The actual hand-off —
  cmd waiting for the process to exit, swapping the .exe, relaunching — was
  run by hand against the built v2.5.0 .exe in a temp folder (it swapped,
  kept `.old.exe`, deleted its script and relaunched; see log.md for the
  `DETACHED_PROCESS` pipe hang that the first attempt found). It is not in
  the suite: it needs a frozen .exe and kills the process it runs in. Nor is
  the macOS branch (open the release page) beyond `webbrowser.open` being the
  standard library.
- **Auto-scroll while dragging (#v2.6.0).** `_autoscroll` reads the canvas's
  real size and the Tk event's pixel position; the suite drives
  `on_canvas_drag` with cell coordinates and never goes through it. It was
  checked once by hand with synthetic events against a 64-cell tree at 48x
  (scrolls up-left at the top-left margin, down-right at the far one). The
  same goes for the hover ghosts (eraser footprint, stick, bar) and the cursor
  shapes over the bar ends and the selection.
- **The label filter menu and the label / colour-picker dialogs** are built by
  the tests' methods (`set_label_filter`, `set_label`, `set_grid_colours`)
  but the menus and dialogs themselves are only opened by hand.
- **Talking to GitHub for real.** Every updater test uses a fake opener. A
  changed API shape or a rate-limited response would surface as the "could
  not reach GitHub" dialog, not as a test failure.
- **Performance is measured, not asserted.** The 64x64 stroke timings in
  `log.md` (#v2.4.0) came from a one-off script; no test fails if rendering
  gets slow again. The structural test — one image item, rows built once —
  guards the specific regression that caused the freeze, not the number.
